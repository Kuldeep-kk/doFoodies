import React from 'react';
import About from "@/app/About/About";

export const metadata = {
    title: 'About: Food Delivery App ',
}

const Page = () => {
    return (
        <About/>
    );
};

export default Page;