import React from 'react';
import Menu from "@/app/Menu/Menu";
export const metadata = {
    title: 'Menu: Food Delivery App ',
}

const Page = () => {
    return (
        <div>
            <Menu/>
        </div>
    );
};

export default Page;